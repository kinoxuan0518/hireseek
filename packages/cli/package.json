{
  "name": "@hireseek/cli",
  "version": "0.1.0",
  "description": "Seeya CLI — 命令行入口（hireseek 命令仍然可用）",
  "type": "module",
  "bin": {
    "seeya": "./dist/index.js",
    "hireseek": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@hireseek/core": "workspace:*",
    "@hireseek/boss-adapter": "workspace:*",
    "@hireseek/maimai-adapter": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0",
    "tsx": "^4.19.0"
  }
}
