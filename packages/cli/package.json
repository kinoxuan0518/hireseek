{
  "name": "@hireseek/cli",
  "version": "0.1.0",
  "description": "🦞 HireSeek CLI — 命令行入口（向后兼容现有 hireseek 命令）",
  "type": "module",
  "bin": {
    "hireseek": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@hireseek/core": "workspace:*",
    "@hireseek/boss-adapter": "workspace:*",
    "@hireseek/maimai-adapter": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0",
    "tsx": "^4.19.0"
  }
}
